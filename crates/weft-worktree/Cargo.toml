[package]
name = "weft-worktree"
version = "0.1.0"
description = "Idempotent git worktree create; path layout is a trait"
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
tokio.workspace = true

[dev-dependencies]
tempfile = "3"
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
