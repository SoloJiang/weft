/** @deprecated Import from `./notificationsCore` (or `./notifications`) instead. */
export {
  DEFAULT_NOTIFY_CATEGORIES,
  DEFAULT_QUIET_HOURS,
  formatQuietTime,
  isInQuietHours,
  NOTIFY_CATEGORIES,
  parseNotifyCategories,
  parseQuietHours,
  parseQuietTime,
  serializeNotifyCategories,
  serializeQuietHours,
  type NotifyCategory,
  type NotifyCategoryFlags,
  type QuietHours,
} from "./notificationsCore";
